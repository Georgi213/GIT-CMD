/*Commit 1-2. leidke SQL-päringus vead (min 1), parandage ja kommenteerige, millised vead on*/
CREATE TABLE instructor (
  ID int primary key identity(1,1), /*fixed*/
  name VARCHAR(20) NOT NULL,
  dept_name VARCHAR(20),
  salary int,/*fixed*/
);

SELECT COUNT(course_id), dept_name
  FROM course
  GROUP BY dept_name;

/*Commit 3.Otsige C#-koodis vigu
{
    Console.WriteLinen "Tere";
    Console.WriteLine("Tere Tulemast C#");
}

/*Commit 4.Otsige html-koodist vigu
<h1 style="font-size:60px;">Heading 1</h8>